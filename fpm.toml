name = "ffilesystem"
description = "Lightweight filesystem and path manipulations"
categories = "io"
version = "4.4.0"

[install]
library = true

[library]
source-dir = "src/common"
include-dir = ["include"]
